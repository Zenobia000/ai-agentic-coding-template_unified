# 🤖 Universal AI Workflow Guide

> 統一多種 AI Copilot 的專業開發工作流程

---

## 🚀 Quick Start

### 1. 初始化設置

```bash
# 安裝依賴
npm install

# 生成 AI 工具配置
npm run ai-sync
```

### 2. 生成配置

所有工具配置將從 `.ai/template/` 自動生成：
- Cursor: `.cursorrules` + `.cursor/`
- Claude Code: `CLAUDE.md` + `.claude/`
- Gemini CLI: `GEMINI.md` + `.gemini/`

### 3. 開始工作流程

| AI 工具 | 初始化命令 | 說明 |
|---------|------------|------|
| **Cursor** | `/van` | Slash command |
| **Claude Code** | `/van` | Slash command |
| **Gemini CLI** | `/van` | Slash command |

---

## 📚 指令快速索引

| 類型 | 指令 | 用途 | 主要交付物 |
|------|------|------|------------|
| **Workflow** | `/van` | 初始化專案 | Memory Bank 結構 |
| | `/plan` | 任務規劃 | tasks.md, WBS |
| | `/creative` | 架構設計 | creative-*.md, API 規格 |
| | `/implement` | 程式實作 | 程式碼, 測試, progress.md |
| | `/reflect` | 回顧總結 | 學習筆記, 改進建議 |
| | `/archive` | 知識歸檔 | runbook.md, 最佳實踐 |
| **Utility** | `/task-next` | PM 建議 | 下一步行動建議 |
| | `/debug` | 系統除錯 | 根因分析, 解決方案 |
| | `/review-code` | 代碼審查 | 審查報告, 改進項目 |
| | `/write-tests` | 測試生成 | 測試案例, 覆蓋率報告 |
| **System** | `/commit` | Git 提交 | Conventional commit message |
| | `/resume` | 恢復狀態 | 狀態摘要, 待辦事項 |
| | `/github` | GitHub 推送 | Git push, PR 連結 |

## 🔄 核心工作流程

### 六階段開發流程
```text
🚀 VAN      → 📋 PLAN     → 🎨 CREATIVE → 🔨 IMPLEMENT → 🪞 REFLECT  → 📦 ARCHIVE
初始化        任務規劃       創意設計       程式實作         回顧反思       文件歸檔
```

### 各階段詳細說明

#### 🚀 VAN - 初始化階段
**目標**: 建立專案記憶庫，確保 AI 協作基礎

**命令輸入範例**:
```bash
/van
# 或帶參數
/van 初始化電商平台專案
```

**執行流程**:
1. 檢查 `memory-bank/` 目錄是否存在
2. 驗證必要檔案完整性
3. 初始化缺失的檔案
4. 更新 `activeContext.md` 記錄當前狀態

**預期記錄**:
```markdown
# activeContext.md
## Current Phase: VAN
- Timestamp: 2024-01-09 10:00
- Status: Project initialized
- Next Action: Define project scope with /plan
```

**交付物**:
- ✅ `memory-bank/` 目錄結構
- ✅ `tasks.md` - 空白任務清單
- ✅ `activeContext.md` - 初始狀態
- ✅ `projectbrief.md` - 專案模板
- ✅ `techContext.md` - 技術棧模板

#### 📋 PLAN - 規劃階段
**目標**: 分解任務，制定開發計畫

**命令輸入範例**:
```bash
/plan 建立用戶認證系統
# 或
/plan 實作購物車功能，支援多幣別結帳
```

**執行流程**:
1. 讀取 `projectbrief.md` 理解專案範圍
2. 將高層次需求拆解成 Epic/Story/Task
3. 評估任務優先級和依賴關係
4. 更新 `tasks.md` 與 `activeContext.md`

**預期記錄**:
```markdown
# tasks.md
## Epic: 用戶認證系統
### User Stories:
- [ ] US-001: 作為用戶，我可以註冊新帳號
- [ ] US-002: 作為用戶，我可以登入系統
### Tasks:
- [ ] TASK-001: 設計用戶資料庫 schema
- [ ] TASK-002: 實作 JWT 認證機制
```

**交付物**:
- ✅ 更新的 `tasks.md` - 完整任務清單
- ✅ 更新的 `projectbrief.md` - 細化的專案範圍
- ✅ WBS (Work Breakdown Structure) - 工作分解結構
- ✅ 優先級矩陣 (Priority Matrix)

#### 🎨 CREATIVE - 創意設計階段
**目標**: 架構設計，技術選型

**命令輸入範例**:
```bash
/creative 設計用戶認證系統架構
# 或
/creative 設計微服務架構，使用 Event Sourcing
```

**執行流程**:
1. 讀取 `tasks.md` 中的特定任務
2. 進行技術研究和方案評估
3. 設計系統架構和數據流
4. 選擇技術棧和工具
5. 創建設計文件

**預期記錄**:
```markdown
# creative-auth-system.md
## 架構設計
### 技術選擇
- Backend: Node.js + Express
- Database: PostgreSQL
- Auth: JWT + bcrypt
### API 設計
- POST /api/auth/register
- POST /api/auth/login
### 資料庫 Schema
```sql
CREATE TABLE users (
  id UUID PRIMARY KEY,
  email VARCHAR(255) UNIQUE,
  password_hash VARCHAR(255)
);
```

**交付物**:
- ✅ `creative-*.md` - 詳細設計文件
- ✅ 架構圖 (Architecture Diagrams)
- ✅ API 規格文件
- ✅ 資料庫設計 (ERD)
- ✅ 更新的 `techContext.md`

#### 🔨 IMPLEMENT - 實作階段
**目標**: 編寫程式碼，實現功能

**命令輸入範例**:
```bash
/implement TASK-001 設計用戶資料庫 schema
# 或
/implement 實作 JWT 認證中間件
```

**執行流程**:
1. 讀取對應的 `creative-*.md` 設計文件
2. 建立檔案結構
3. 編寫程式碼 (TDD 優先)
4. 執行測試
5. 更新進度

**預期記錄**:
```markdown
# progress.md
## 2024-01-09
### 完成項目
- ✅ TASK-001: 用戶資料庫 schema
  - 建立 migration 檔案
  - 實作 User model
  - 單元測試 100% 覆蓋
### 進行中
- 🔄 TASK-002: JWT 認證機制 (60%)
```

**交付物**:
- ✅ 程式碼檔案 (Source Code)
- ✅ 單元測試 (Unit Tests)
- ✅ 整合測試 (Integration Tests)
- ✅ 更新的 `progress.md`
- ✅ 更新的 `tasks.md` (標記完成項目)

#### 🪞 REFLECT - 回顧階段
**目標**: 檢討成果，總結經驗

**命令輸入範例**:
```bash
/reflect 今天的實作進度
# 或
/reflect Sprint 1 的成果與教訓
```

**執行流程**:
1. 讀取 `progress.md` 與 `tasks.md`
2. 分析完成率和問題點
3. 總結學習經驗
4. 提出改進建議
5. 更新相關文件

**預期記錄**:
```markdown
# activeContext.md
## Reflection Summary
### 成就
- 完成 3/5 任務 (60%)
- 測試覆蓋率達 95%
### 挑戰
- JWT refresh token 實作複雜度超出預期
### 學習
- 使用 Redis 管理 session 更有效率
### 下一步
- 優化認證流程
- 增加錯誤處理
```

**交付物**:
- ✅ 進度報告 (Progress Report)
- ✅ 問題清單與解決方案
- ✅ 學習筆記 (Lessons Learned)
- ✅ 更新的 `activeContext.md`
- ✅ 改進建議 (Improvement Suggestions)

#### 📦 ARCHIVE - 歸檔階段
**目標**: 整理文件，知識保存

**命令輸入範例**:
```bash
/archive 用戶認證模組
# 或
/archive Sprint 1 完整文檔
```

**執行流程**:
1. 收集所有相關文件
2. 整理和分類
3. 提取可重用的知識
4. 建立文檔索引
5. 生成最佳實踐

**預期記錄**:
```markdown
# runbook.md
## 用戶認證模組
### 快速開始
1. 安裝依賴: `npm install`
2. 設定環境變數: `cp .env.example .env`
3. 執行 migration: `npm run migrate`
### 最佳實踐
- 使用 bcrypt rounds >= 10
- JWT 過期時間設 15 分鐘
- Refresh token 存在 Redis
### 常見問題
- Q: Token 過期怎麼處理？
  A: 使用 refresh token 機制...
```

**交付物**:
- ✅ `runbook.md` - 操作手冊
- ✅ 文檔索引 (Documentation Index)
- ✅ 最佳實踐指南 (Best Practices)
- ✅ FAQ 文件
- ✅ 可重用的程式碼片段 (Code Snippets)
- ✅ 完整的 README.md

---

## 🛠️ Utility Commands - 輔助工具詳解

### 📊 /task-next - PM 建議工具
**目標**: 分析當前狀態，建議下一個高優先級任務

**命令輸入範例**:
```bash
/task-next
# 或指定範圍
/task-next 只看前端任務
```

**執行流程**:
1. 讀取 `tasks.md` 分析未完成任務
2. 評估依賴關係和阻塞因素
3. 考慮優先級和時程
4. 提供行動建議

**預期輸出**:
```markdown
【下一步建議】
優先任務: TASK-002 JWT 認證機制
原因: TASK-001 已完成，無其他阻塞
預計工時: 4 小時
依賴項: 無
```

### 🐛 /debug - 除錯模式
**目標**: 系統化的除錯流程

**命令輸入範例**:
```bash
/debug API 回傳 401 錯誤
/debug 資料庫連線逾時
```

**執行流程**:
1. 收集錯誤資訊
2. 形成假設 (Hypothesis)
3. 設計驗證實驗
4. 執行測試
5. 記錄結果

**預期輸出**:
```markdown
【除錯分析】
問題: API 401 錯誤
假設1: Token 過期
測試: 檢查 token 時間戳記 ✓
假設2: Token 格式錯誤
測試: 驗證 JWT 結構 ✗
根因: Bearer token 前綴缺失
解決方案: headers['Authorization'] = `Bearer ${token}`
```

### 🔍 /review-code - 代碼審查
**目標**: 深度代碼品質檢查

**命令輸入範例**:
```bash
/review-code auth/login.js
/review-code 檢查所有新增的檔案
```

**執行流程**:
1. 靜態分析 (Linting)
2. 複雜度評估
3. 安全性檢查
4. 最佳實踐驗證
5. 效能分析

**預期輸出**:
```markdown
【代碼審查報告】
檔案: auth/login.js
✅ 通過項目:
- 無 ESLint 錯誤
- 測試覆蓋率 95%
⚠️ 建議改進:
- 第 45 行: 考慮使用 async/await
- 第 67 行: 密碼複雜度驗證不足
🔴 必須修正:
- 第 23 行: SQL Injection 風險
```

### ✅ /write-tests - 測試撰寫
**目標**: 生成完整的測試案例

**命令輸入範例**:
```bash
/write-tests UserService
/write-tests login 功能的 edge cases
```

**執行流程**:
1. 分析目標程式碼
2. 識別測試場景
3. 生成測試案例
4. 執行測試
5. 計算覆蓋率

**預期輸出**:
```javascript
// Generated test cases
describe('UserService', () => {
  describe('login', () => {
    it('should return token for valid credentials', async () => {
      // Test implementation
    });
    it('should reject invalid password', async () => {
      // Test implementation
    });
    it('should handle rate limiting', async () => {
      // Test implementation
    });
  });
});
// Coverage: 92% statements, 88% branches
```

---

## 🎯 System Commands - 系統指令詳解

### 📝 /commit - Git 提交助手
**目標**: 生成符合規範的 commit message

**命令輸入範例**:
```bash
/commit
# 或指定類型
/commit feat
```

**執行流程**:
1. 分析 git diff
2. 識別變更類型
3. 提取關鍵改動
4. 生成 conventional commit

**預期輸出**:
```
feat(auth): implement JWT refresh token mechanism

- Add refresh token generation
- Store refresh tokens in Redis
- Implement token rotation strategy
- Add tests for refresh flow

Closes #123
```

### 🔄 /resume - 上下文恢復
**目標**: 從 Memory Bank 恢復工作狀態

**命令輸入範例**:
```bash
/resume
# 或指定時間點
/resume 昨天的狀態
```

**執行流程**:
1. 讀取所有 Memory Bank 檔案
2. 分析當前階段
3. 識別進行中的任務
4. 恢復上下文
5. 提供狀態摘要

**預期輸出**:
```markdown
【狀態恢復完成】
當前階段: IMPLEMENT
活動任務: TASK-002 JWT 認證機制
完成進度: 60%
最後更新: 2024-01-09 15:30
下一步: 完成 refresh token 邏輯
待解決: Token 過期處理
```

### 🚀 /github - GitHub 操作
**目標**: 執行完整的 Git 推送流程

**命令輸入範例**:
```bash
/github
# 或指定分支
/github feature/auth
```

**執行流程**:
1. git add -A
2. 生成 commit message
3. git commit
4. git push
5. 創建 PR (如需要)

**預期輸出**:
```bash
Changes staged: 5 files
Commit created: feat(auth): implement login
Pushed to: origin/feature/auth
PR URL: https://github.com/user/repo/pull/42
```

---

## 🧠 Memory Bank 系統

### 核心檔案結構
```
memory-bank/
├── tasks.md              # 📋 任務清單 (單一真相來源)
├── activeContext.md      # 🎯 當前焦點
├── projectbrief.md       # 📄 專案概述
├── techContext.md        # 🔧 技術背景
├── progress.md           # 📈 進度追蹤
├── creative-*.md         # 🎨 設計文件
└── runbook.md           # 📚 操作手冊
```

### 檔案職責

| 檔案 | 職責 | 更新時機 |
|------|------|----------|
| **tasks.md** | 任務管理 | 所有階段 |
| **activeContext.md** | 當前狀態 | 切換階段時 |
| **projectbrief.md** | 專案定義 | 初始化, 需求變更 |
| **techContext.md** | 技術資訊 | 技術選型, 架構變更 |
| **progress.md** | 進度記錄 | 實作階段 |

---

## 🛠️ 多工具設定

### 工具特定檔案

| AI 工具 | 配置檔案 | 生成方式 |
|---------|----------|----------|
| **Cursor** | `.cursorrules`, `.cursor/` | `npm run ai-sync:cursor` |
| **Claude Code** | `CLAUDE.md`, `.claude/` | `npm run ai-sync:claude` |
| **Gemini CLI** | `GEMINI.md`, `.gemini/` | `npm run ai-sync:gemini` |

### 統一配置系統
```
.ai/                    # 🤖 通用配置目錄
├── config.yaml        # 📋 主配置
├── rules/              # 📏 通用規則
│   ├── global.md      # 🌍 全域規則
│   ├── frontend.md    # 🎨 前端規則
│   └── backend.md     # ⚙️ 後端規則
├── commands/           # ⚔️ 通用命令
│   ├── workflow/      # 六階段流程
│   ├── utility/       # 輔助工具
│   └── system/        # 系統功能
├── agents/            # 🤝 AI 代理定義
│   ├── code-reviewer.md
│   └── test-runner.md
└── template/          # 📝 配置模板
    ├── .cursorrules   # Cursor 模板
    ├── CLAUDE.md      # Claude 模板
    └── GEMINI.md      # Gemini 模板
```

---

## 📋 最佳實踐

### ✅ 建議做法

1. **階段順序遵循**
   - 不要跳過階段
   - 每個階段完成後再進入下一階段
   - 確保前置條件滿足

2. **Memory Bank 維護**
   - 頻繁更新 activeContext.md
   - 保持 tasks.md 為最新狀態
   - 定期備份重要決策

3. **工具使用一致性**
   - 使用相同的工作流程
   - 保持術語統一
   - 記錄工具特定的技巧

### ❌ 避免的做法

1. **不要跳過 Memory Bank 驗證**
2. **不要在工作流程外修改 Memory Bank**
3. **不要忽略階段前置條件**
4. **不要同時使用多個 AI 工具操作同一任務**

---

## 🔧 工具切換

### 專案中途切換 AI 工具

```bash
# 1. 確保當前狀態同步到 Memory Bank
[current-tool] reflect

# 2. 生成新工具配置
npm run ai-sync:[new-tool]

# 3. 使用新工具初始化
[new-tool] van

# 4. 繼續工作流程
[new-tool] [next-phase]
```

### 團隊協作

```bash
# 1. 確保 Memory Bank 同步
git add memory-bank/
git commit -m "Update Memory Bank state"

# 2. 團隊成員使用各自偏好的工具
# Alice 使用 Cursor
/van

# Bob 使用 Claude Code
/van

# Charlie 使用 Gemini CLI
/van
```

---

## 🆘 故障排除

### 常見問題

#### Q: Memory Bank 檔案遺失
```bash
# 重新初始化
/van  # 在 AI 工具中執行
```

#### Q: AI 工具無法讀取配置
```bash
# 重新生成工具配置
npm run ai-sync
# 或針對特定工具
npm run ai-sync:cursor
npm run ai-sync:claude
npm run ai-sync:gemini
```

#### Q: 工作流程狀態不同步
```bash
# 驗證系統狀態
npm run ai-verify

# 手動同步 Memory Bank
[your-tool] reflect
```

#### Q: 跨工具協作出現衝突
```bash
# 統一到 Memory Bank 狀態
git checkout memory-bank/
[your-tool] van
```

---

## 📚 進階使用

### 自定義工作流程階段

1. 在 `.ai/config.yaml` 中添加新階段
2. 在 `.ai/commands/` 中定義階段命令
3. 重新生成工具配置

### 專案模板

```bash
# 建立新的專案模板
cp -r .ai/ ../new-project/.ai/
cd ../new-project
npm install && npm run ai-sync
```

### 自動化腳本

```bash
# 每日同步腳本
#!/bin/bash
git pull
npm run ai-verify
[your-tool] reflect
git add .
git commit -m "Daily sync: $(date)"
git push
```

---

## 🌟 成功案例

### 使用統一工作流程的優勢

1. **一致性**: 無論使用哪種 AI 工具，都有相同的專業體驗
2. **可移植性**: 可以在工具間自由切換而不丟失上下文
3. **協作性**: 團隊可以使用不同 AI 工具但共享相同狀態
4. **可擴展性**: 容易添加新的 AI 工具支援

### 適用場景

- **個人項目**: 想要專業級 AI 協作體驗
- **團隊開發**: 需要統一的 AI 輔助開發流程
- **多工具環境**: 在不同情境下使用不同 AI 工具
- **學習實驗**: 比較不同 AI 工具的特性

---

**🎯 現在開始你的 AI 協作之旅吧！**

```bash
npm install && npm run ai-sync
```